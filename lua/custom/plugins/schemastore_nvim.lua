-- requires vscode-langservers-extracted installed on system to work
-- install with npm: npm i -g vscode-langservers-extracted
-- https://github.com/hrsh7th/vscode-langservers-extracted
return {
  'b0o/schemastore.nvim',
  config = function()
    -- require('lspconfig').jsonls.setup {
    -- ...
    -- }
  end,
}
